import React from 'react';
import s from './productsPageTitle.module.css';

const ProductsPageTitle = () => {
    return (
        <h1 className={s.title}>
            Список товаров
        </h1>
    );
}

export default ProductsPageTitle;
